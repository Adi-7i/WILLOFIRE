/**
 * UploadPdfDto
 *
 * Placeholder DTO for the PDF upload endpoint.
 * Currently, the endpoint only expects a multipart `file` upload.
 * If we add metadata fields (e.g. `title`, `description`) in the future,
 * they will be validated here.
 */
export class UploadPdfDto { }
