/**
 * infrastructure/redis/index.ts
 *
 * Redis infrastructure layer — caching and session storage.
 *
 * Phase 2 will add:
 *  - RedisModule (ioredis via @nestjs-modules/ioredis or custom provider)
 *  - CacheService wrapper for get/set/del with TTL support
 *  - Used by: AuthModule (refresh token store), any caching needs
 */

// export { RedisModule } from './redis.module';
// export { CacheService } from './cache.service';

export { };
