/**
 * infrastructure/storage/index.ts
 *
 * File storage infrastructure layer.
 *
 * Phase 2 will add:
 *  - StorageModule with provider interface (local / S3 / GCS)
 *  - StorageService with upload/download/delete methods
 *  - Configured via STORAGE_PROVIDER env var for easy swapping
 *  - Used by: PdfModule for storing uploaded PDFs
 */

// export { StorageModule } from './storage.module';
// export { StorageService } from './storage.service';

export { };
