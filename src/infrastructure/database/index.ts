/**
 * infrastructure/database/index.ts
 *
 * Database infrastructure layer — MongoDB via Mongoose.
 *
 * Phase 2 will add:
 *  - DatabaseModule (MongooseModule.forRootAsync with ConfigService)
 *  - Connection health check
 *  - Retry logic on connection failure
 */

// export { DatabaseModule } from './database.module';

export { };
