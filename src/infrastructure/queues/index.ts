/**
 * infrastructure/queues/index.ts
 *
 * BullMQ job queue infrastructure layer.
 *
 * Phase 2 will add:
 *  - Dedicated queue for PDF processing (cpu-heavy, async)
 *  - Dedicated queue for AI calls (rate-limited, async)
 *  - Queue health dashboard integration (Bull Board)
 *  - Dead-letter queue handling for failed jobs
 */

// export { PdfProcessingQueue } from './pdf-processing.queue';
// export { AiQueue } from './ai.queue';

export { };
