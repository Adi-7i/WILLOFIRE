/**
 * Queue Names
 *
 * Centralized list of BullMQ queue names to prevent typos across
 * the app (used when defining queues, workers, and queue events).
 */

export const PDF_PROCESSING_QUEUE = 'pdf-processing';
export const MCQ_GENERATION_QUEUE = 'mcq-generation';
export const ANSWER_EVALUATION_QUEUE = 'answer-evaluation';
