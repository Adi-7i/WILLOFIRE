/**
 * common/pipes/index.ts
 *
 * Barrel file for all custom NestJS pipes.
 *
 * Phase 2+ will add:
 *  - ParseObjectIdPipe — validates and transforms MongoDB ObjectId strings
 *  - FileSizePipe — validates uploaded file size constraints
 */

// export { ParseObjectIdPipe } from './parse-object-id.pipe';

export { };
