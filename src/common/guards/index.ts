/**
 * common/guards/index.ts — barrel export for all guards.
 */
export { JwtAuthGuard } from './jwt-auth.guard';

// export { RolesGuard } from './roles.guard';   // Phase 3 — RBAC
// export { ThrottlerGuard } from './throttler.guard'; // Phase 3
