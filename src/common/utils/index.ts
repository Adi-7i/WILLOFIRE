/**
 * common/utils/index.ts
 *
 * Barrel file for all shared utility functions.
 * Utilities are pure functions — no side effects, no DI dependencies.
 *
 * Phase 2+ will add:
 *  - paginate() — standardized pagination response builder
 *  - hashPassword() / comparePassword() — bcrypt wrappers
 *  - generateSlug() — URL-safe slug generation from strings
 *  - formatApiResponse() — consistent success response wrapper
 */

// export { paginate } from './paginate.util';
// export { hashPassword, comparePassword } from './password.util';

export { };
