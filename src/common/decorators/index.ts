/**
 * common/decorators/index.ts — barrel export for all custom decorators.
 * Add new decorators here as the project grows.
 */
export { CurrentUser } from './current-user.decorator';
export { Public, IS_PUBLIC_KEY } from './public.decorator';

// export { Roles } from './roles.decorator'; // Phase 3 — RBAC
